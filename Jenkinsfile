@Library('conservify') _

conservifyProperties()

timestamps {
    node () {
        // conservifyBuild(name: 'app-protocol', archive: false)
        stage ('git') {
            checkout scm
        }
    }
}
